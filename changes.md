# user name tooltip has to be always visible
# use cool names for users
# use chips for user names for online users
# prabably we can use material
# connection modes yjs / crd supports (eg. socket, webrtc and what else?)